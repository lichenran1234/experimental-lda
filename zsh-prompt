if [ -f /usr/share/scm/scm-prompt.sh ]; then
  source /usr/share/scm/scm-prompt.sh
fi
if [ -f /opt/facebook/sl/share/scm-prompt ]; then
  source /opt/facebook/sl/share/scm-prompt
fi
if [ -f /opt/fbsl/share/scm/scm-prompt.sh ]; then
  source /opt/fbsl/share/scm/scm-prompt.sh
fi

export PROMPT='%{$fg[cyan]%}%/ $fg[yellow]%$(_scm_prompt) $(git_prompt_info)$fg[white]%}%1{---------------------%}
$fg_bold[white]%}%h %(?:%{$fg_bold[green]%}%1{===>%}:%{$fg_bold[red]%}%1{===>%}) %{$reset_color%}'
